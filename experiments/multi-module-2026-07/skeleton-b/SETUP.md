# Condition B setup

Beyond skeleton-a, this skeleton differs by exactly two things:

1. `npm install @fonderie/core` (already in package.json; `npm install` restores it)
2. The Fonderie skill: copy `.claude/skills/fonderie/` from this repository's
   root into this directory before running.
