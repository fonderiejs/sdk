---
"@fonderie/courier": patch
---

Align the `email-registration` plain-text heading with its HTML: the text body
opened with `Welcome` while the HTML `<h1>` read `Welcome aboard`. Both now read
`Welcome aboard`.
