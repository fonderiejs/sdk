---
"@fonderie/courier": patch
---

Change the email `brandAccent` (the thin top rule) from the mint `#00d294` to the
near-black `#171717`, matching the primary — the email frame is now fully
monochrome.
